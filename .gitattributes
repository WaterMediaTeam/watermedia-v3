# THE PENTESTING CORPUS IS BYTE-EXACT BY CONSTRUCTION: SEVERAL PAYLOADS ARE PLAIN-TEXT ENOUGH FOR
# GIT TO SNIFF THEM AS TEXT (NETPBM HEADERS, SVG ENTITY BOMBS), AND EOL CONVERSION WOULD REWRITE THE
# VERY BYTES UNDER TEST — A CHECKOUT ON WINDOWS WOULD SHIFT EVERY OFFSET AND SILENTLY DEFUSE THEM.
src/test/resources/pentest/** -text
