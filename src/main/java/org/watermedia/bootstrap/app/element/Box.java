package org.watermedia.bootstrap.app.element;

/**
 * A bare decorated rectangle with no content of its own — it just paints the {@link Element} decor
 * (background, border, glow, shadow). Handy as a dialog scrim, a spacer, a coloured panel, or any place
 * that needs a plain filled region.
 */
public final class Box extends Element<Box> {
}
