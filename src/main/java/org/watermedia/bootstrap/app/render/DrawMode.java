package org.watermedia.bootstrap.app.render;

/**
 * Backend-neutral primitive mode used by the UI render engine.
 */
public enum DrawMode {
    TRIANGLES,
    TRIANGLE_FAN,
    LINES,
    LINE_LOOP,
    LINE_STRIP
}
