package org.watermedia.bootstrap.app.view;

/**
 * A bare decorated rectangle with no content of its own — it just paints the {@link View} decor
 * (background, border, glow, shadow). Handy as a dialog scrim, a spacer, a coloured panel, or any place
 * that needs a plain filled region.
 */
public final class Box extends View<Box> {
}
